# set configurations for Commit Guardian here, see the examples below.
# @param disable rule for Commit Guardian, rule set here will be disabled, use comma to seperate each.
# @param ignores rule for Commit Guardian, file set here will be ignore, use comma to seperate each.
[Rule]
# disable = 
# ignores = 

# set configurations for Pylint here, see the examples below.
# @param env working environment of Pylint, use base as default if not set.
# @param disable message control for Pylint checks, message set here will be disabled, use comma to seperate each.
# @param importcheck flag to determine if do import error checking on Mac, default equals to False
[Pylint]
env = GUI
disable = E0401
